"use client";
import { useEffect, useState } from "react";
import Link from "next/link";
import { PhoneOff } from "lucide-react";
import { CallSession } from "@/lib/types";
export default function CallLive({callId}:{callId:string}){const [session,setSession]=useState<CallSession|null>(null);const [error,setError]=useState("");useEffect(()=>{let alive=true;const poll=async()=>{const r=await fetch(`/api/calls/${callId}`,{cache:"no-store"});if(r.ok&&alive)setSession(await r.json());};poll();const timer=setInterval(poll,2500);return()=>{alive=false;clearInterval(timer)}},[callId]);async function end(){const r=await fetch(`/api/calls/${callId}/hangup`,{method:"POST"});if(r.ok)setSession(await r.json());else setError((await r.json()).error)}if(!session)return <main className="page"><p>Loading call session…</p></main>;return <main className="page"><div className="page-header"><div><p className="eyebrow">JobShop AI Recruiter</p><h1 className="page-title">Live screening call</h1><p className="page-subtitle">{session.candidate.jobRole} · {session.candidate.phone}</p></div><div className="status"><span className="status-dot"/> {session.status}</div></div><div className="call-layout"><section className="panel call-panel"><div className="call-meta"><div className="call-person"><span className="avatar">{session.candidate.name.slice(0,1).toUpperCase()}</span><div><strong>{session.candidate.name}</strong><small>{session.candidate.city||"Indian candidate"}</small></div></div><button className="button" onClick={end}><PhoneOff size={16}/> End call</button></div><div className="transcript">{session.transcript.length?session.transcript.map((entry,i)=><div className={`message ${entry.speaker.toLowerCase()}`} key={`${entry.at}-${i}`}><div><div className="message-meta">{entry.speaker}</div><div className="message-bubble">{entry.text}</div></div></div>):<div className="empty-transcript">The call will appear here as Twilio receives each spoken answer.</div>}</div>{session.status==="COMPLETED"&&<Link className="button button-primary" href={`/calls/${callId}`}>Open final report</Link>}{error&&<p className="error">{error}</p>}</section><aside className="panel"><p className="eyebrow">AI extracted details</p><div className="info-list">{Object.entries(session.extractedData).map(([key,value])=><div className="info-row" key={key}><span>{key.replace(/[A-Z]/g,m=>` ${m}`).toUpperCase()}</span><strong>{String(value)}</strong></div>)}{!Object.keys(session.extractedData).length&&<p className="form-note">Candidate details will update after spoken answers are recognized.</p>}</div></aside></div></main>}
