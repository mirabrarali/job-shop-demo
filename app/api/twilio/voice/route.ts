import { NextResponse } from "next/server";
import { getSession, updateSession } from "@/lib/calls/store";
import { gatherSpeech, voiceResponse } from "@/lib/twilio/twiml";
export async function POST(request:Request){const url=new URL(request.url);const id=url.searchParams.get("callId")||"";const session=getSession(id);const response=voiceResponse();if(!session){response.say("This call session is no longer available.");return new NextResponse(response.toString(),{headers:{"Content-Type":"text/xml"}});}const greeting=`Hi, I am calling from JobShop. Am I speaking with ${session.candidate.name}?`;updateSession(id,{status:"CONNECTED",transcript:[...session.transcript,{speaker:"AI",text:greeting,at:new Date().toISOString()}]});return new NextResponse(gatherSpeech(response,greeting,id).toString(),{headers:{"Content-Type":"text/xml"}});}
