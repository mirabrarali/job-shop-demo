import twilio from "twilio";
import { twilioConfigured } from "./validation";
export function getTwilioClient() { if (!twilioConfigured()) throw new Error("Twilio is not configured. Add Twilio credentials to make a real call."); return twilio(process.env.TWILIO_ACCOUNT_SID!, process.env.TWILIO_AUTH_TOKEN!); }
export async function startTwilioCall(to: string, callId: string) { const base = process.env.NEXT_PUBLIC_BASE_URL?.trim().replace(/\/$/, ""); if (!base || !/^https:\/\//i.test(base)) throw new Error("NEXT_PUBLIC_BASE_URL must be your public HTTPS Vercel URL."); return getTwilioClient().calls.create({ to, from: process.env.TWILIO_PHONE_NUMBER!, url: `${base}/api/twilio/voice?callId=${encodeURIComponent(callId)}`, method:"POST", statusCallback:`${base}/api/twilio/status?callId=${encodeURIComponent(callId)}`, statusCallbackMethod:"POST", statusCallbackEvent:["initiated","ringing","answered","completed"] }); }
